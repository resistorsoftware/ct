- if Spree::Config[:allow_openid]
  - unless params[:user] && params[:user][:openid_identifier]
    %p= t(:or)
  %p
    = f.label :openid_identifier, t('OpenID', :default => 'OpenID')
    %br
    = f.text_field :openid_identifier, :class => 'title openid_url'
